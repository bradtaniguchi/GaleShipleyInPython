#!/usr/bin/python3
import random  # to generate random preferences

class Person():
    '''
    A person class, defined by their prefernce list, boolean if they are male, 
    their name, and if they are engaged
    :param name: string - name of person
    :param man: boolean - if this person is a male
    '''
    def __init__(self, name, male):
        self.name = name #string
        self.pref = [] #emtpy list
        self.male = male #boolean
        self.engaged = True #boolean

    def addPref(list):
        self.pref = list

    def _generateRandPref(listOfOtherGender):
        self.pref = random.shuffle(listOfOtherGender) # randomly shuffle items

def main():
    print("Starting program...")
    # create 3 different men
    man1 = Person('A', True)
    man2 = Person('B', True)
    man3 = Person('C', True)

    # create 3 different women
    woman1 = Person('X', False)
    woman2 = Person('Y', False)
    woman3 = Person('Z', False)

    men = [man1, man2, man3]
"""
    #define preferences
    man1.addPref([woman1, woman2, woman3])
    man2.addPref([woman3, woman2, woman1])
    man3.addPref([woman2, woman1, woman3])

    woman1.addPref([man1, man2, man3])
    woman2.addPref([man2, man3, man1])
    woman3.addPref([man3, man1, man2])
    # all persons are free
"""
    while(man1.engaged || man2.engaged || man3.engaged):
        if(!men.length == 0):
            man = men.pop #is this right, gets first man, in list
            man.pref.pop #get first person on list
   ###TODO 
if __name__ == '__main__':
    main()
